# 最终上传前质量检查

- X 锚点：5 条均有 X 单帖截图、作者、日期和浏览量信息。
- 视频：ffprobe 兼容包装器确认 5 条均有 video/audio 且时长 7 秒；渲染帧抽查确认源帧均为 1080x1920。
- 封面：均由独立 cover config 和 people/company-first 背景渲染，不从 MP4 抽帧。
- 包结构：每个主题文件夹仅保留 `视频.mp4` 与 `封面.jpg`，记录材料进入 `_记录/`。
